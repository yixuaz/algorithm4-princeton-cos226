package part2.week3.maxflow;

/**
 * Perfect matchings in k-regular bipartite graphs. Suppose that there are nn men and nn women at a dance and that
 * each man knows exactly kk women and each woman knows exactly kk men (and relationships are mutual).
 * Show that it is always possible to arrange a dance so that each man and woman are matched with someone they know.
 */
public class PerfectMatchingInKBipartite {
    // previous n/2 is men, last n/2 is women, relationship.length = n/2 + n/2 = n
    // if men know women, then relationships[i][j] = 1;
    public static boolean solve(int[][] relationships, int k) {
        // TODO: ADD YOUR CODE HERE
        return false;
    }
}
